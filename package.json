{
  "name": "mjml-email-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:emails": "node scripts/build-emails.js",
    "dev": "npm run build:emails && vite",
    "build": "npm run build:emails && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "mjml": "^5.2.2",
    "nodemailer": "^8.0.8",
    "vite": "^8.0.14"
  }
}
